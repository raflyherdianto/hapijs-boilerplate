# ==============================================
# 🚀 BACKEND CAPSTONE PROJECT - ENV TEMPLATE
# ==============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control!

# ==============================================
# 🌍 APPLICATION ENVIRONMENT
# ==============================================
NODE_ENV=development
# Options: development, test, production

# ==============================================
# 🖥️ SERVER CONFIGURATION
# ==============================================
PORT=3000
HOST=localhost
# Set HOST=0.0.0.0 for Docker or remote access

# ==============================================
# 🗄️ DATABASE CONFIGURATION (Development)
# ==============================================
DB_USERNAME=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=backend_capstone
DB_HOST=127.0.0.1
DB_PORT=5432
DB_LOGGING=false
# Set DB_LOGGING=true to see SQL queries in console

# ==============================================
# 🧪 TEST DATABASE CONFIGURATION (Optional)
# ==============================================
DB_USERNAME_TEST=postgres
DB_PASSWORD_TEST=your_test_database_password_here
DB_NAME_TEST=backend_capstone_test
DB_HOST_TEST=127.0.0.1
DB_PORT_TEST=5432

# ==============================================
# 🚀 PRODUCTION DATABASE CONFIGURATION
# ==============================================
# Required only in production environment
DB_USERNAME_PROD=your_production_db_username
DB_PASSWORD_PROD=your_production_db_password
DB_NAME_PROD=your_production_db_name
DB_HOST_PROD=your_production_db_host
DB_PORT_PROD=5432

# ==============================================
# 🔐 JWT AUTHENTICATION
# ==============================================
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_complex
# Generate a strong secret key for production!
# Example: openssl rand -base64 64

# ==============================================
# 🔧 ADDITIONAL CONFIGURATION
# ==============================================
# Force database authentication in production (use with caution)
RUN_DB_AUTH_IN_PROD=false

# ==============================================
# 📝 DEVELOPMENT NOTES
# ==============================================
# 1. Replace all placeholder values with your actual configuration
# 2. Keep your .env file secure and never commit it to version control
# 3. For production, use environment variables or secure secret management
# 4. Generate a strong JWT_SECRET for production environments
# 5. Ensure database credentials are correct for your PostgreSQL setup
